# Pending promotion note

The RapidOCR frame adapter is implemented on `feature/rapidocr-frame-adapter` and remains a candidate until full pull-request CI passes on Python 3.10 and 3.12. On promotion, merge this status into `docs/PRD_PROGRESS.md` and remove this temporary note.
